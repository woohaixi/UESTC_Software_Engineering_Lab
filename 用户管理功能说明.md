# 用户管理功能说明

## 功能概述

系统已实现完整的用户账户管理功能，包括启用、禁用、删除等操作。

## 管理员账户

**默认管理员账户：**
- 用户名：`admin`
- 密码：`admin123`

**注意：** 首次运行程序时会自动创建管理员账户。

## 用户管理功能

### 1. 查看用户列表

管理员登录后，点击"管理面板"按钮，在"用户管理"标签页可以看到：

**用户列表显示信息：**
- 用户名
- 邮箱
- 角色（管理员/普通用户）
- 状态（正常/禁用）
- MFA状态（已启用/未启用）
- 最后登录时间
- 注册时间

### 2. 启用用户

**操作步骤：**
1. 进入管理面板 → 用户管理
2. 在"用户名"输入框中输入要启用的用户名
3. 选择"激活"操作
4. 点击"执行操作"按钮

**效果：**
- 用户状态变为"active"（正常）
- 用户可以正常登录系统

### 3. 禁用用户

**操作步骤：**
1. 进入管理面板 → 用户管理
2. 在"用户名"输入框中输入要禁用的用户名
3. 选择"禁用"操作
4. 点击"执行操作"按钮

**效果：**
- 用户状态变为"disabled"（禁用）
- 用户无法登录系统
- 登录时会提示"账户已被disabled，请联系管理员"

### 4. 删除用户

**操作步骤：**
1. 进入管理面板 → 用户管理
2. 在"用户名"输入框中输入要删除的用户名
3. 选择"删除"操作
4. 点击"执行操作"按钮

**效果：**
- 用户账户被永久删除
- 无法恢复（请谨慎操作）

**限制：**
- 不能删除管理员账户（admin）

### 5. 刷新用户列表

点击"刷新用户列表"按钮可以实时更新用户列表数据。

## 登录日志功能

在管理面板的"登录日志"标签页可以查看：

**日志信息：**
- 用户名
- 登录状态（成功/失败）
- IP地址
- User Agent（用户代理）
- 登录时间

**功能：**
- 显示最近50条登录记录
- 点击"刷新日志"按钮更新数据
- 可用于安全审计和问题排查

## 用户角色说明

### 管理员（admin）
- 可以访问管理面板
- 可以查看所有用户信息
- 可以管理用户账户（启用/禁用/删除）
- 可以查看登录日志
- 可以使用医疗问答功能

### 普通用户（user）
- 只能使用医疗问答功能
- 看不到"管理面板"按钮
- 无法访问用户管理功能

## 安全特性

1. **账户状态检查**
   - 登录时自动检查账户状态
   - 禁用账户无法登录

2. **管理员保护**
   - 管理员账户不能被删除
   - 防止误操作导致系统无管理员

3. **操作日志**
   - 所有登录尝试都会被记录
   - 包括成功和失败的登录

4. **实时更新**
   - 操作后自动刷新用户列表
   - 确保显示最新状态

## 使用示例

### 示例1：禁用违规用户

```
场景：用户 "testuser" 违反使用规定，需要禁用账户

步骤：
1. 使用 admin 账户登录
2. 点击"管理面板"
3. 在用户管理页面，输入用户名：testuser
4. 选择操作：禁用
5. 点击"执行操作"
6. 系统提示：用户 testuser 状态已更新为: disabled
7. 用户列表中该用户状态显示为"禁用"
```

### 示例2：恢复被禁用的用户

```
场景：用户 "testuser" 申诉成功，需要恢复账户

步骤：
1. 使用 admin 账户登录
2. 点击"管理面板"
3. 在用户管理页面，输入用户名：testuser
4. 选择操作：激活
5. 点击"执行操作"
6. 系统提示：用户 testuser 状态已更新为: active
7. 用户可以正常登录
```

### 示例3：删除测试账户

```
场景：删除不再需要的测试账户

步骤：
1. 使用 admin 账户登录
2. 点击"管理面板"
3. 在用户管理页面，输入用户名：test123
4. 选择操作：删除
5. 点击"执行操作"
6. 系统提示：用户 test123 已删除
7. 用户列表中不再显示该用户
```

## 数据库表结构

### users 表
```sql
- id: 用户ID（主键）
- username: 用户名（唯一）
- password: 密码哈希
- email: 邮箱
- role: 角色（admin/user）
- status: 状态（active/disabled）
- mfa_enabled: 是否启用MFA
- last_login: 最后登录时间
- created_at: 创建时间
```

### login_logs 表
```sql
- id: 日志ID（主键）
- username: 用户名
- success: 是否成功（0/1）
- ip_address: IP地址
- user_agent: 用户代理
- login_time: 登录时间
- logout_time: 退出时间
- session_duration: 会话时长
```

## 注意事项

1. **谨慎删除**：删除操作不可恢复，请确认后再执行
2. **管理员保护**：系统至少保留一个管理员账户
3. **及时刷新**：执行操作后建议刷新列表查看最新状态
4. **日志审计**：定期查看登录日志，发现异常及时处理
5. **权限管理**：只有管理员可以访问用户管理功能

## 运行方式

```bash
python auth_app.py
```

访问浏览器中显示的地址（通常是 http://127.0.0.1:7860）
